"""Semantic processing service for MetricMind chat questions."""


def process_question(question: str) -> str:
    """Process a user's question before it is sent to the AI service."""
    return question